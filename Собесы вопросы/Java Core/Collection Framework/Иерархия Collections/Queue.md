**`Queue<E>`** — _очередь FIFO_
- `addFirst()`
- `addLast()`
- `removeFirst()`
- `removeLast()`

#### Наследники
**`PriorityQueue<E>`** - элементы _извлекаются в порядке их приоритета_ (определяется по __компаратору__ или естественному порядку).
`Deque`
**`BlockingQueue<E>`** -   потокобезопасная очередь.
